@page
@model eCashier.Pages.CategoryPages.DetailsModel

@{
    ViewData["Title"] = "Details";
}
<div class="card">
    <h3 class="card-header">Details</h3>
    <div class="card-body">
        <h6>Category</h6>
        <hr />
        <div>
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Category.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Category.Name)
                </dd>
            </dl>
        </div>
        <div>
            <a class="btn btn-warning" asp-page="./Edit" asp-route-id="@Model.Category.Id">Edit</a> |
            <a asp-page="./Index">Back to List</a>
        </div>
    </div>
</div>



